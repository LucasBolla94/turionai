services:
  turion:
    image: node:20-alpine
    working_dir: /app
    command: ["sh", "-c", "npm install && npm run dev"]
    volumes:
      - ./:/app
      - ./state:/app/state
      - ./logs:/app/logs
    restart: unless-stopped
