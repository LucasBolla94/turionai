services:
  turion:
    image: node:20-alpine
    working_dir: /app
    command: ["sh", "-c", "npm install && npm run dev"]
    volumes:
      - ./:/app
      - ./state:/app/state
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
